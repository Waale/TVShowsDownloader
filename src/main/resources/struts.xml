<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.5//EN"
        "http://struts.apache.org/dtds/struts-2.5.dtd">

<struts>
	<constant name="struts.devMode" value="true" />

	<package name="tvshows-downloader" namespace="/" extends="struts-default">
		<action name="home" class="tvshowsdownloader.actions.HomeAction">
			<result name="success">home.jsp</result>
		</action>
	</package>

	<package name="tvshows-downloader-api" namespace="/api" extends="json-default">
		<interceptors>
		    <interceptor-stack name="apiStack">
		        <interceptor-ref name="json">
		            <param name="enableSMD">true</param>
		        	<param name="noCache">true</param>
		        	<param name="excludeNullProperties">true</param>
		        </interceptor-ref>
		    </interceptor-stack>
		</interceptors>

		<default-interceptor-ref name="apiStack" />
		
		<global-results>
			<result name="success" type="json" />
			<result name="bad-request-error" type="json">
				<param name="statusCode">400</param>
				<param name="ignoreHierarchy">false</param>
				<param name="includeProperties">^actionErrors.*</param>
			</result>
			<result name="unauthorized-error" type="json">
				<param name="statusCode">401</param>
				<param name="ignoreHierarchy">false</param>
				<param name="includeProperties">^actionErrors.*</param>
			</result>
			<result name="not-found-error" type="json">
				<param name="statusCode">404</param>
				<param name="ignoreHierarchy">false</param>
				<param name="includeProperties">^actionErrors.*</param>
			</result>
			<result name="error" type="json">
				<param name="statusCode">500</param>
				<param name="ignoreHierarchy">false</param>
				<param name="includeProperties">^actionErrors.*</param>
			</result>
		</global-results>
		
		<action name="download-episodes" class="tvshowsdownloader.actions.DownloadEpisodesAction" />
		<action name="get-episodes" class="tvshowsdownloader.actions.GetEpisodesAction"/>

	</package>
</struts>